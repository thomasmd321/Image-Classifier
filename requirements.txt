# Upper bounds keep a future major release from breaking the project; constraints.txt has the exact
# versions CI and the Docker image are tested with (pip install -r requirements.txt -c constraints.txt)
torch>=1.10,<3
torchvision>=0.11,<1
Pillow>=8,<13
numpy>=1.19,<3
matplotlib>=3.3,<4
