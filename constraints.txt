# Exact versions the project is tested with (Python 3.11). CI and the Docker image install these, so a new
# release of a dependency can't change or break them without a deliberate update here.
# Use: pip install -r requirements-dev.txt -c constraints.txt
torch==2.14.0
torchvision==0.29.0
pillow==12.3.0
numpy==2.4.6
matplotlib==3.11.2
gradio==6.28.0
huggingface_hub==1.32.0
onnx==1.23.0
onnxruntime==1.30.0
tensorboard==2.21.0
pytest==9.1.1
ruff==0.16.8
